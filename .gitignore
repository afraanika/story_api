#Logs
logs
*.log
npm-debug.log*
# Runtime data
pids
*.pid
*.seed
# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov
# Coverage directory used by tools like istanbul
coverage
# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt
# node-waf configuration
.lock-wscript
# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release
# Dependency directories
node_modules
jspm_packages
# Optional npm cache directory
.npm
# Optional REPL history
.node_repl_history
# Buffers for emacs
*~
*#
# Our conf file
routes/util/conf.js
#idea directory
.idea
# don't git the certs from test serve at WebHuset
cert/fullchain1.pem
cert/privkey1.pem
.nyc_output
# don't git the Codeship AES encryption key
codeship.aes
# don't git the unencrypted dockercfg file
dockercfg
# never git firebase admin sdk key
firebase-admin-sdk-key/*.json
#ignore .pem, .pem.BAK and .key
*.pem
*.key
*.pem.BAK
*.pem.BAK-290618
*.key.BAK
#ignore uploads directory
uploads
.vscode/launch.json
dumb_*.js